import 'package:flutter/material.dart';

class ComponentB extends StatelessWidget {
  const ComponentB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('COMPONENT B');
  }
}
