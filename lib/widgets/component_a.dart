import 'package:flutter/material.dart';

class ComponentA extends StatelessWidget {
  const ComponentA({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('COMPONENT A');
  }
}
